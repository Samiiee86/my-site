export const SPOTLIGHT_R = 260;

// Base layer: what you see normally.
export const HERO_IMAGE = "/hero-base.png";

// Reveal layer: plays inside the cursor spotlight.
export const HERO_VIDEO = "/hero-reveal.mp4";

// How far the artwork is pushed down the screen (% of viewport height), so the
// ridge clears the headline. The spotlight mask compensates by the same amount.
export const MEDIA_SHIFT_PCT = 20;
